import { Screen, Text } from '@/ui';

/** Placeholder until Settings is built. */
export default function SettingsTab() {
  return (
    <Screen>
      <Text variant="title">Settings</Text>
    </Screen>
  );
}
