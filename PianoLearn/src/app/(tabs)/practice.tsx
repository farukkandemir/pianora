import { Screen, Text } from '@/ui';

/** Placeholder until the Practice tab is designed in code. */
export default function PracticeTab() {
  return (
    <Screen>
      <Text variant="title">Practice</Text>
      <Text tone="muted">Pick a piece from the Library to start.</Text>
    </Screen>
  );
}
